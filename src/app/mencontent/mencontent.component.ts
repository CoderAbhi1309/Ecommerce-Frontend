import { Component, Input, OnInit } from '@angular/core';

@Component({
  selector: 'app-mencontent',
  templateUrl: './mencontent.component.html',
  styleUrls: ['./mencontent.component.css']
})
export class MencontentComponent implements OnInit {
  
  // @Input() title!: string;
  // @Input() content!: string;
  // @Input() imagesource!: string;
  constructor() { }

  ngOnInit(): void {
  }

}
